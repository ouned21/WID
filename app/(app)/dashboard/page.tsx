'use client';

import DashboardFree from './DashboardFree';

// Dashboard unique : vue Score.
// Les anciens skins (Vivid, Dark, Clean, Galaxy) ont été retirés
// pour simplifier l'expérience — un seul langage visuel dans l'app.
export default function DashboardPage() {
  return <DashboardFree />;
}
